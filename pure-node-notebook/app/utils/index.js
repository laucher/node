const url=require("url");

const utils={

    // getStaticPathFromUrl:(url)=>{
    //     if(typeof url === "string")
    //         url=url.parse(url);
    //     return url.pathname;
    // }

}

module.exports=utils;